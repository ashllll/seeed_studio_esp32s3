# 系统流程图与 UI 布局更新

## 系统流程图
（此处为系统流程图内容，保持不变）

## UI 布局更新记录

### 卡片式布局更新 (2025-04-20)
- **主页面**：更新为卡片式布局，显示为带有边框和标题的卡片，包含当前温度、目标温度和功率百分比等信息。
- **校准页面**：更新为卡片式布局，显示为带有边框和标题的卡片，包含当前读数、校准目标和偏差信息。
- **系统信息页面**：更新为卡片式布局，显示为带有边框和标题的卡片，包含版本信息、系统运行时间和 PID 参数。
- **错误页面**：更新为卡片式布局，显示为带有边框和标题的卡片，包含错误代码、错误消息和安全状态提示。
- **菜单页面**：更新为卡片式布局，显示为带有边框和标题的卡片，菜单项重新排列以适应卡片设计。

这些修改提供了更现代化的外观和更直观的交互体验，基于 Astra UI Lite 框架的逻辑。

## 代码流程图

以下是系统的代码流程图，描述了主要模块的逻辑流程：

```
初始化:
  - 系统启动
  - 初始化硬件（温度传感器、PWM 控制器、显示屏）
  - 初始化 UI 界面和 PID 控制器

主循环:
  - 读取用户输入（旋转编码器、按键）
  - 更新系统状态（根据用户输入调整目标温度或菜单选择）
  - 读取当前温度
  - 执行 PID 控制算法（计算输出功率）
  - 更新 PWM 输出
  - 根据当前页面绘制 UI 界面
    - 主页面：显示温度和功率
    - 菜单页面：显示菜单项和选择器
    - 其他页面：根据功能显示相关信息
  - 处理错误状态（如果有）
  - 延时并返回主循环
```

## 电路连接图

以下是系统的电路连接图，描述了主要硬件组件的连接方式：

```
ESP32S3 开发板:
  - GPIO 5 (SDA) -> OLED 显示屏 SDA
  - GPIO 6 (SCL) -> OLED 显示屏 SCL
  - GPIO 18 -> 温度传感器 (MAX6675) CS
  - GPIO 19 -> 温度传感器 (MAX6675) SCK
  - GPIO 21 -> 温度传感器 (MAX6675) MISO
  - GPIO 4 -> PWM 输出 (连接到加热元件控制电路)
  - GPIO 34 -> 旋转编码器 A 相
  - GPIO 35 -> 旋转编码器 B 相
  - GPIO 36 -> 旋转编码器按键
```

**注意**：请确保所有连接都使用合适的电阻和保护电路，避免直接连接可能导致的硬件损坏。

## 使用说明

1. **硬件连接**：按照上述电路连接图连接所有硬件组件，确保电源电压和电流符合设备要求。
2. **软件安装**：使用 PlatformIO 环境，将代码上传到 ESP32S3 开发板。
3. **操作指南**：
   - **主页面**：显示当前温度和目标温度，旋转编码器可调整目标温度，单击进入菜单。
   - **菜单页面**：旋转编码器选择菜单项，单击确认选择，双击返回上一级。
   - **校准页面**：调整温度偏移以校准传感器读数。
   - **系统信息页面**：显示版本和运行时间等信息。
   - **错误页面**：显示错误代码和消息，长按重置系统。
4. **注意事项**：请勿在未连接温度传感器的情况下运行加热控制功能，以免发生过热危险。

## 免责说明

本项目及其代码、硬件设计和文档仅供学习和研究目的提供。作者不对因使用本项目而导致的任何直接或间接损害负责，包括但不限于设备损坏、数据丢失或人身伤害。在使用本项目之前，请确保您已充分了解相关风险，并采取必要的安全措施。使用本项目即表示您同意自行承担所有风险。

如果您有任何问题或需要进一步支持，请联系项目维护者。